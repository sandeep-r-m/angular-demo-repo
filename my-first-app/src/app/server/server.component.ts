import { Component } from "@angular/core";

@Component(
    {
        // selector:'[app-server]' by attribute e.g <div app-server></div>
        // selector:'[.app-server]' by class e.g <div class="app-server"></div>
        selector: 'app-server', // by element e.g. <app-server></app-server>
        templateUrl: './server.component.html'
    }
)

export class ServerComponent {

}